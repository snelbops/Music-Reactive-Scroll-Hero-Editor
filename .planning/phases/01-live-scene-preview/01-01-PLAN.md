---
phase: 01-live-scene-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scroll-hero-editor/src/packages/useKickDrumData.ts
  - scroll-hero-editor/src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx
  - scroll-hero-editor/src/presets/ParticleLab/TouchTexture.ts
  - scroll-hero-editor/src/presets/ParticleLab/index.ts
  - scroll-hero-editor/public/github-test-app/index.html
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02
  - SCAF-03

must_haves:
  truths:
    - "useKickDrumData hook exists at src/packages/useKickDrumData.ts with a deferred comment and no wiring into UI"
    - "All six required dependencies are resolvable in node_modules with no missing peer dep warnings"
    - "GithubTestParticleField.tsx and TouchTexture.ts are present under src/presets/ParticleLab/ with correct relative imports"
    - "The Classic preset static bundle (index.html + css + scripts + images) is present under scroll-hero-editor/public/github-test-app/"
  artifacts:
    - path: "scroll-hero-editor/src/packages/useKickDrumData.ts"
      provides: "Deferred audio hook"
      contains: "// deferred: audio sync — v2"
    - path: "scroll-hero-editor/src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx"
      provides: "R3F Orbit particle component"
    - path: "scroll-hero-editor/src/presets/ParticleLab/TouchTexture.ts"
      provides: "Touch interaction texture"
    - path: "scroll-hero-editor/src/presets/ParticleLab/index.ts"
      provides: "Clean re-export barrel"
    - path: "scroll-hero-editor/public/github-test-app/index.html"
      provides: "Classic preset static bundle entry point"
  key_links:
    - from: "scroll-hero-editor/src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx"
      to: "scroll-hero-editor/src/presets/ParticleLab/TouchTexture.ts"
      via: "relative import '../TouchTexture'"
      pattern: "from '../TouchTexture'"
---

<objective>
Scaffold the particle preset source files and verify the dependency baseline so Phase 1 rendering tasks have everything they need.

Purpose: SCAF-01 ensures the audio hook is present but clearly deferred. SCAF-02 confirms all six required packages are installed. SCAF-03 copies the particle-lab source files and the Classic preset static bundle into the editor project so subsequent plans can import and serve them.
Output: useKickDrumData.ts annotated; src/presets/ParticleLab/ populated with GithubTestParticleField + TouchTexture + barrel index; public/github-test-app/ populated with the Classic preset bundle.
</objective>

<execution_context>
@/Users/johnsnel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnsnel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-scene-preview/01-RESEARCH.md

<interfaces>
<!-- Key interfaces needed by executor. Extracted from codebase. -->

From scroll-hero-editor/src/packages/useKickDrumData.ts (existing file — only ADD comment):
```typescript
export interface KickDrumData {
    beats: number[];
    waveform: Float32Array;
    isReady: boolean;
}
export function useKickDrumData(audioUrl: string | null): KickDrumData
```

From Presets/particle-lab-package/src/GithubTest/GithubTestParticleField.tsx (copy source):
```typescript
interface GithubTestParticleFieldProps {
    imageUrl: string;
    theme: 'light' | 'dark';
    rotationSpeed?: number;   // default 0.1
    depth?: number;           // default 2.0
    size?: number;            // default 1.4
    touchRadius?: number;     // default 0.15
}
export const GithubTestParticleField = ({ imageUrl, theme, rotationSpeed, depth, size, touchRadius }: GithubTestParticleFieldProps) => JSX.Element
```
Import inside this file: `import { TouchTexture } from '../TouchTexture';`
After copy to src/presets/ParticleLab/GithubTest/, this resolves to src/presets/ParticleLab/TouchTexture.ts — no path change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate useKickDrumData as deferred (SCAF-01) and verify installed deps (SCAF-02)</name>
  <files>scroll-hero-editor/src/packages/useKickDrumData.ts</files>
  <action>
Open scroll-hero-editor/src/packages/useKickDrumData.ts. The file already exists with the correct exports. Add a single-line comment at the very top of the file (above the imports):

```
// deferred: audio sync — v2
```

Do not change anything else in the file.

After saving, verify all six required packages are installed by checking node_modules. Run from within scroll-hero-editor/:
```bash
ls node_modules/@theatre/studio node_modules/@theatre/r3f node_modules/@react-three/fiber node_modules/@react-three/drei node_modules/three node_modules/gsap
```
All six directories must exist. If any is missing run `npm install` from scroll-hero-editor/. Per the research, all six are already installed at the correct versions (SCAF-02 is already satisfied).

Also run `npx tsc --noEmit` from scroll-hero-editor/ to confirm zero TypeScript errors before proceeding.
  </action>
  <verify>
    <automated>cd "/Users/johnsnel/Github/Music-Reactive Scroll Hero Editor/scroll-hero-editor" && grep "deferred: audio sync" src/packages/useKickDrumData.ts && ls node_modules/@theatre/studio node_modules/@theatre/r3f node_modules/@react-three/fiber node_modules/@react-three/drei node_modules/three node_modules/gsap && echo "SCAF-01 and SCAF-02 verified"</automated>
  </verify>
  <done>useKickDrumData.ts has the deferred comment on line 1. All six node_modules directories exist. tsc --noEmit exits with code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Copy particle-lab source files and Classic preset public bundle (SCAF-03)</name>
  <files>
    scroll-hero-editor/src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx
    scroll-hero-editor/src/presets/ParticleLab/TouchTexture.ts
    scroll-hero-editor/src/presets/ParticleLab/index.ts
    scroll-hero-editor/public/github-test-app/index.html
  </files>
  <action>
Run these shell commands from the repository root (the parent of both scroll-hero-editor/ and Presets/):

```bash
# Create target directories
mkdir -p "scroll-hero-editor/src/presets/ParticleLab/GithubTest"

# Copy R3F particle component (import path '../TouchTexture' is correct after copy)
cp "Presets/particle-lab-package/src/GithubTest/GithubTestParticleField.tsx" \
   "scroll-hero-editor/src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx"

# Copy TouchTexture (imported by the particle component as '../TouchTexture')
cp "Presets/particle-lab-package/src/TouchTexture.ts" \
   "scroll-hero-editor/src/presets/ParticleLab/TouchTexture.ts"

# Copy Classic preset static bundle (includes index.html + css + scripts + images)
cp -r "Presets/particle-lab-package/public/github-test-app" \
   "scroll-hero-editor/public/github-test-app"
```

After copying, create a barrel index file at scroll-hero-editor/src/presets/ParticleLab/index.ts:

```typescript
export { GithubTestParticleField } from './GithubTest/GithubTestParticleField';
```

Do NOT copy GithubTestView.tsx — that is the standalone app component with its own sidebar and is not needed; only GithubTestParticleField.tsx and TouchTexture.ts are used.

After copying, run `npx tsc --noEmit` from scroll-hero-editor/ to check for TypeScript errors in the copied files. Common fixes if errors appear:
- Convert any `enum` declarations to union string literals (TypeScript erasableSyntaxOnly: true)
- Add `import type` for any type-only imports (verbatimModuleSyntax: true)
- Remove any unused imports (noUnusedLocals: true)

Fix any errors before marking this task done.
  </action>
  <verify>
    <automated>cd "/Users/johnsnel/Github/Music-Reactive Scroll Hero Editor/scroll-hero-editor" && test -f src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx && test -f src/presets/ParticleLab/TouchTexture.ts && test -f src/presets/ParticleLab/index.ts && test -f public/github-test-app/index.html && test -d public/github-test-app/images && echo "SCAF-03 file structure verified" && npx tsc --noEmit && echo "TypeScript clean"</automated>
  </verify>
  <done>All four paths exist. public/github-test-app/images/ directory exists with sample PNG files. npx tsc --noEmit exits with code 0 (no TypeScript errors in copied files).</done>
</task>

</tasks>

<verification>
Run from scroll-hero-editor/:
1. `grep "deferred: audio sync" src/packages/useKickDrumData.ts` — confirms SCAF-01
2. `ls node_modules/@theatre/studio node_modules/three node_modules/gsap` — confirms SCAF-02
3. `test -f src/presets/ParticleLab/GithubTest/GithubTestParticleField.tsx && test -f public/github-test-app/index.html && echo ok` — confirms SCAF-03
4. `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- useKickDrumData.ts has `// deferred: audio sync — v2` on line 1
- All 6 npm packages present in node_modules
- GithubTestParticleField.tsx and TouchTexture.ts copied to correct paths with unchanged relative import
- barrel index.ts exports GithubTestParticleField
- public/github-test-app/ contains index.html and images/ with sample PNGs
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-scene-preview/01-01-SUMMARY.md`
</output>
