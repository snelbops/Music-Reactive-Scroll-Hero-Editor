---
phase: 02-playhead-transport-and-motion-recording
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [PROG-01, PROG-02, PROG-03, PROG-04, TL-01, TL-02, TL-03, TL-04, TL-05, TL-06, REC-01, REC-02, REC-03, REC-04, REC-05]

must_haves:
  truths:
    - "All Phase 2 success criteria are verifiable by the creator in the running browser"
    - "Human has confirmed the full record-play-scrub workflow end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 2 workflow in the browser.

Purpose: Phase 2 delivers real interactivity (playback, recording, scrubbing) — the only way to confirm it works is by running through the full workflow. This checkpoint is the acceptance gate.
Output: Creator confirms all 5 success criteria from ROADMAP.md Phase 2.
</objective>

<execution_context>
@/Users/johnsnel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnsnel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 implementation:
- SceneAdapter contract (PROG-01) — setProgress drives active scene
- Progress overlay in viewport (PROG-02) — numeric readout replaces DEBUG label
- Draggable viewport scrub handle (PROG-03/04) — pointer capture, snaps playhead
- Play/Stop/Loop transport (TL-03/04/05) — Stop halts, Loop wraps
- Reactive TIME display (TL-06) — updates each frame via onChange
- Scroll POS lane with timeline playhead drag (TL-01/02)
- Arm Record with lane highlight (REC-01)
- Keyframe write during record+play (REC-02)
- Playhead restore on stop (REC-03)
- Keyframe dots on Scroll POS lane (REC-04)
- Draggable keyframe dots (REC-05)
  </what-built>
  <how-to-verify>
Run `npm run dev` from `scroll-hero-editor/` and open http://localhost:5173

**Test 1 — Progress overlay (PROG-02):**
- The viewport shows a numeric progress readout (e.g. `0.000`) in the top-left corner
- It should NOT say "DEBUG: Scroll Progress"
  Test Feeback: This says 0.000 and updates correctly when play is pressed and playhead moves

**Test 2 — Viewport scrub handle (PROG-03/04):**
- Drag the white thumb on the right-side vertical bar up and down
  Test Feeback: Working
- Progress overlay value should update in real time (0.000 → 1.000)
  Test Feeback: Working
- Timeline playhead (red line) should snap to the matching position
  Test Feeback: Working

**Test 3 — Play/Stop/Loop transport (TL-03/04/05):**
- Click Play (triangle) — TIME display in transport bar should count up (e.g. 00:00:00.0 → 00:00:01.0)
  Test Feeback: Working
- Click Stop (square) — playhead halts at current position, does NOT jump to 00:00:00.0
  Test Feeback: Working
- Return to 0 by dragging the scrub handle to the top
  Test Feeback: Working
- Enable Loop (click "LOOP" — it should turn purple)
  Test Feeback: Working. Note, however, that a full loop features should be included later, so to be able to select a part in the timeline, like mark in and mark out, but in a similar style to that of Abletong where you can select a part of the timeline and loop it.Ableton has a loop selector, see the screenshot @feedback/screenshots/AbletonLoop.png. Both ends are easily draggable.
- Click Play — let the sequence run to the end — it should wrap back to 0:00 and continue
  Test Feeback: Working

**Test 4 — Full recording workflow (REC-01 through REC-05):**
1. Seek to position 0 (drag scrub handle to top)
2. Click the Arm Record button (circle icon) — it should glow red; Scroll POS lane label should show a purple highlight
Test Feeback: Working
3. Click Play
4. While playing, drag the viewport scrub handle up and down to vary progress
5. Click Stop (or click the Arm Record button again)
6. Observe: playhead should return to position 0 (pre-record position)
7. Observe: purple dots should appear on the Scroll POS lane at the positions where you moved the handle
8. Drag one of the purple dots left or right — it should move and the curve should update
IMPORTANT: Test Feedback: Record behaviour is exactly the same as play. "Implement Cavalry-like timeline: playhead at current frame/time drives all procedural animation; clips/keybars limit layer visibility; behaviors offset/scale time from composition Time input; support scrubbing, markers, keyframing attributes."

**Test 5 — Timeline playhead drag (TL-02):**
- Click and drag anywhere in the timeline track area (right of the lane labels)
- The red playhead line should follow the mouse
- Progress overlay should update to match
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what you observed</resume-signal>
</task>
TEst Feedback: working

</tasks>

<verification>
Human verified all 5 Phase 2 success criteria in the running application.
</verification>

<success_criteria>
- Creator confirms all 5 tests pass in the browser
- Phase 2 requirements PROG-01 through REC-05 marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After human approval:
1. Update `.planning/REQUIREMENTS.md` — mark PROG-01 through REC-05 as `[x]`
2. Update `.planning/ROADMAP.md` — mark Phase 2 as complete with today's date
3. Update `.planning/STATE.md` — set Current focus to Phase 3, update progress bar
4. Create `.planning/phases/02-playhead-transport-and-motion-recording/02-06-SUMMARY.md`
</output>
