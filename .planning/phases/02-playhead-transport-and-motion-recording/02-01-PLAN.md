---
phase: 02-playhead-transport-and-motion-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scroll-hero-editor/src/preview/SceneAdapter.ts
  - scroll-hero-editor/src/store/useStore.ts
  - scroll-hero-editor/src/theatre/TheatreSync.tsx
  - scroll-hero-editor/src/preview/Viewport.tsx
autonomous: true
requirements: [PROG-01, PROG-02]

must_haves:
  truths:
    - "The active scene responds to setProgress(0-1) via a uniform SceneAdapter contract"
    - "Viewport displays a non-interactive numeric progress overlay (e.g. 0.472) replacing the debug overlay"
    - "TheatreSync drives setSceneProgress from onValuesChange, calling the active adapter"
    - "Playing the sequence drives the active scene continuously via the adapter contract"
  artifacts:
    - path: "scroll-hero-editor/src/preview/SceneAdapter.ts"
      provides: "SceneAdapter interface + OrbitAdapter + ClassicAdapter"
      exports: ["SceneAdapter", "OrbitAdapter", "ClassicAdapter"]
    - path: "scroll-hero-editor/src/store/useStore.ts"
      provides: "isLoop, recordStartPosition, setSceneProgress fields"
      contains: "setSceneProgress"
    - path: "scroll-hero-editor/src/theatre/TheatreSync.tsx"
      provides: "onValuesChange calls setSceneProgress"
  key_links:
    - from: "scroll-hero-editor/src/theatre/TheatreSync.tsx"
      to: "scroll-hero-editor/src/store/useStore.ts"
      via: "setSceneProgress in onValuesChange callback"
      pattern: "setSceneProgress"
    - from: "scroll-hero-editor/src/store/useStore.ts"
      to: "scroll-hero-editor/src/preview/SceneAdapter.ts"
      via: "activeAdapter.setProgress(p) inside setSceneProgress action"
      pattern: "activeAdapter"
---

<objective>
Define the SceneAdapter contract, wire it into the Zustand store and TheatreSync, and upgrade the viewport debug overlay to a styled progress readout.

Purpose: Every downstream plan (scrub handle, recording, transport) calls setSceneProgress — this plan creates that unified path and makes progress visible in the viewport.
Output: SceneAdapter.ts with OrbitAdapter and ClassicAdapter; useStore extended with isLoop, recordStartPosition, setSceneProgress; TheatreSync calling setSceneProgress; progress overlay replacing debug label.
</objective>

<execution_context>
@/Users/johnsnel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johnsnel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-playhead-transport-and-motion-recording/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From scroll-hero-editor/src/store/useStore.ts (current):
```typescript
type PresetId = 'orbit' | 'classic';

interface EditorState {
  isPlaying: boolean;
  setIsPlaying: (playing: boolean) => void;
  isRecording: boolean;
  setIsRecording: (rec: boolean) => void;
  recordedEvents: RecordedEvent[];
  scrollProgress: number;       // 0–1 Zustand bridge for React UI
  setScrollProgress: (progress: number) => void;
  activePreset: PresetId;
  setActivePreset: (preset: PresetId) => void;
  // ... other existing fields
}
```

From scroll-hero-editor/src/theatre/TheatreSync.tsx (current):
```typescript
// onValuesChange currently calls setScrollProgress only:
return scrollControlsObj.onValuesChange((values) => {
  setScrollProgress(values.position);
});
```

From scroll-hero-editor/src/preview/Viewport.tsx (current):
```typescript
// Debug overlay to REPLACE (keep pointer-events-none):
<div className="absolute top-4 left-4 z-50 bg-black/80 text-green-400 font-mono text-xs px-2 py-1 rounded border border-green-400/30 shadow-[0_0_10px_rgba(34,197,94,0.2)] pointer-events-none">
  DEBUG: Scroll Progress = {scrollProgress.toFixed(3)}
</div>
```

From scroll-hero-editor/src/presets/ParticleLab (GithubTestParticleField):
- The component accepts imageUrl and theme props.
- It uses uAssemble (0→1 assembly animation) and rotationSpeed internally.
- For PROG-01, wire progress to control uAssemble via a ref or prop.
- GithubTestParticleField does not currently accept a `progress` prop — add one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SceneAdapter contract and extend Zustand store</name>
  <files>
    scroll-hero-editor/src/preview/SceneAdapter.ts
    scroll-hero-editor/src/store/useStore.ts
  </files>
  <action>
Create `src/preview/SceneAdapter.ts` with:
- `export interface SceneAdapter { setProgress(p: number): void; }`
- `export class OrbitAdapter implements SceneAdapter` — constructor receives a `setUAssemble: (v: number) => void` callback (will be wired in Viewport via useRef/useState); `setProgress(p)` calls `setUAssemble(p)`.
- `export class ClassicAdapter implements SceneAdapter` — `setProgress(p)` posts `window.frames` postMessage if an iframe ref is provided, or is a no-op graceful fallback. Constructor accepts `iframeRef: React.RefObject<HTMLIFrameElement | null>`. Implementation: `this.iframeRef.current?.contentWindow?.postMessage({ type: 'setProgress', value: p }, '*')`.

Extend `src/store/useStore.ts` — additive edits only, never replace existing keys:
- Add `isLoop: boolean` (default `false`) + `setIsLoop: (v: boolean) => void`
- Add `recordStartPosition: number` (default `0`) + `setRecordStartPosition: (v: number) => void`
- Add `activeAdapter: SceneAdapter | null` (default `null`) + `setActiveAdapter: (adapter: SceneAdapter | null) => void`
- Add `setSceneProgress: (p: number) => void` — this action calls `get().activeAdapter?.setProgress(p)` AND `set({ scrollProgress: p })`. Import `SceneAdapter` at top of useStore.ts. Use the Zustand `create<EditorState>((set, get) => ({...}))` signature — add `get` parameter to the factory function since `setSceneProgress` needs it.

TypeScript: run `npx tsc --noEmit` from `scroll-hero-editor/` and fix any errors before committing.
  </action>
  <verify>
    <automated>cd "/Users/johnsnel/Github/Music-Reactive Scroll Hero Editor/scroll-hero-editor" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>SceneAdapter.ts exists with exported interface + two adapter classes; useStore compiles with isLoop, recordStartPosition, activeAdapter, setSceneProgress; npx tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SceneAdapter into TheatreSync and upgrade Viewport progress overlay</name>
  <files>
    scroll-hero-editor/src/theatre/TheatreSync.tsx
    scroll-hero-editor/src/preview/Viewport.tsx
  </files>
  <action>
In `TheatreSync.tsx`:
- Replace the `onValuesChange` callback: instead of calling only `setScrollProgress`, call `useStore.getState().setSceneProgress(values.position)`. Since `setSceneProgress` now calls both `setScrollProgress` and `activeAdapter?.setProgress`, this is the only call needed.
- Keep the RAF loop's existing `setScrollProgress` calls AS-IS for now (they will be upgraded in Plan 03 when the loop gains loop/stop-at-position fixes).
- Add `isLoop` from the store (will be used in Plan 03): import it but do not wire yet — just read it with `const isLoop = useStore(s => s.isLoop)` so it's available for Plan 03's diff.

In `Viewport.tsx`:
- Add OrbitAdapter wiring: create a `uAssembleRef = useRef(0)` and a `setUAssemble` callback that forwards to `GithubTestParticleField`'s `progress` prop. Add a `progress` prop to the `<GithubTestParticleField>` render call: `progress={scrollProgress}`. This requires adding a `progress?: number` prop to `GithubTestParticleField` — do this by updating the component's prop type in `src/presets/ParticleLab/GithubTestParticleField.tsx` (or its index.tsx export entry, wherever the component prop type is declared). In the component, use `progress ?? 0` to drive `uAssemble` uniform value. If the component uses a Three.js material uniform `uAssemble`, set it via `useEffect` watching the `progress` prop: `materialRef.current.uniforms.uAssemble.value = progress`.
- Create `OrbitAdapter` and set it as `activeAdapter` in a `useEffect` on mount (and when `activePreset` changes): when `activePreset === 'orbit'` call `setActiveAdapter(new OrbitAdapter(setUAssemble))`; when `activePreset === 'classic'` call `setActiveAdapter(new ClassicAdapter(iframeRef))`. Import OrbitAdapter and ClassicAdapter from `../preview/SceneAdapter`. Import setActiveAdapter from useStore.
- Replace the DEBUG overlay with a styled progress readout (PROG-02): same position and z-index, but use white text, no green border. Content: `{scrollProgress.toFixed(3)}` with a small label `PROGRESS`. Keep `pointer-events-none`. Example: `<div className="absolute top-4 left-4 z-50 bg-black/70 text-white font-mono text-sm px-3 py-1.5 rounded pointer-events-none"><span className="text-editor-accent-purple">{scrollProgress.toFixed(3)}</span><span className="text-[9px] text-gray-500 ml-1">PROGRESS</span></div>`

Note: If GithubTestParticleField's uAssemble is complex to extract, use a simpler approach for the Orbit adapter: the OrbitAdapter's setProgress just calls setScrollProgress directly (progress is already in Zustand and drives the overlay). The key contract is the interface; the visual feedback from progress overlay (PROG-02) is the critical deliverable. Do not spend time reverse-engineering Three.js shader internals — the interface is what matters here for Phase 2.

TypeScript: run `npx tsc --noEmit` and fix any errors.
  </action>
  <verify>
    <automated>cd "/Users/johnsnel/Github/Music-Reactive Scroll Hero Editor/scroll-hero-editor" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>TheatreSync calls setSceneProgress (not just setScrollProgress) from onValuesChange; Viewport shows numeric progress overlay (not "DEBUG: Scroll Progress"); activeAdapter is set on mount; npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<verification>
- `src/preview/SceneAdapter.ts` exists with exported SceneAdapter interface, OrbitAdapter, ClassicAdapter
- `useStore.ts` has isLoop, recordStartPosition, activeAdapter, setSceneProgress without breaking existing fields
- `TheatreSync.tsx` calls `setSceneProgress` from onValuesChange
- `Viewport.tsx` shows progress overlay with numeric value (not the old DEBUG label)
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- SceneAdapter interface exported from src/preview/SceneAdapter.ts
- useStore compiles with 4 new fields (isLoop, recordStartPosition, activeAdapter, setSceneProgress)
- Progress overlay visible in Viewport showing 0.000 format
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-playhead-transport-and-motion-recording/02-01-SUMMARY.md`
</output>
